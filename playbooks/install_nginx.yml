---
- hosts: node8
  tasks:
    - name: add repository nginx-release
      yum: name="http://nginx.org/packages/rhel/7/$basearch/"
    - name: install nginx
      yum:
        name: nginx
        state: latest
    - name: start nginx
      service: name=nginx enabled=yes state=started
...
